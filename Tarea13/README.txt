Ejercicio1.c
	
	Input:
		A partir de un n dado, escrito en el código

	Output:
		Se crea un vetor de flotantes de tamaño n, se imprime un cuarto de la mitad del vector, se imprime el último cuarto de elementos del vector y se implrimen lso elementos de atrás para adelante.

Ejercicio2.c

	Se hace un vector de más de 1000 elementos complejos, la norma de estos complejos es 1. Además se regresa el ángulo entre 50 complejos de los últimos 50 elementos complejos del vector creado al inicio.

Ejercicio3.c
	
	Se imprime el tamaño de int, float, double, char, int*, float*, double*, char*